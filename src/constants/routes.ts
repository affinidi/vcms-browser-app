interface Routes {
  ROOT: string,
  LOGIN: string,
  SIGNUP: string,
  SIGNUP_CONFIRM: string,
  ISSUER: string,
  VERIFIER: string,
  HOLDER: string
}

export const routes: Routes = {
  ROOT: '/',
  LOGIN: '/login',
  SIGNUP: '/signup',
  SIGNUP_CONFIRM: '/confirm-signup',
  ISSUER: '/issuer',
  VERIFIER: '/verifier',
  HOLDER: '/holder',
}
