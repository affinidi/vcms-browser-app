interface Routes {
  ROOT: string,
  LOGIN: string,
  SIGNUP: string,
  SIGNUP_CONFIRM: string,
}

export const routes: Routes = {
  ROOT: '/',
  LOGIN: '/login',
  SIGNUP: '/signup',
  SIGNUP_CONFIRM: '/confirm-signup'
}
